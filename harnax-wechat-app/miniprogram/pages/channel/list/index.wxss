/* ---- Channel List — Modern Minimal ---- */
.ch-card {
  background: #FFFFFF;
  border-radius: var(--radius-lg);
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: var(--shadow);
  border: 1rpx solid var(--border-color);
}
.ch-card:active { background: #F7F8FA; }
.ch-card__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12rpx;
}
.ch-card__name {
  font-size: 32rpx;
  font-weight: 600;
  color: #1D2129;
}
.ch-card__meta {
  display: flex;
  gap: 12rpx;
  margin-bottom: 12rpx;
}
.ch-card__tag {
  font-size: 22rpx;
  color: #4F46E5;
  background: #EEF2FF;
  border-radius: var(--radius-full);
  padding: 4rpx 18rpx;
  font-weight: 500;
}
.ch-card__tag--gray {
  color: #86909C;
  background: #F2F3F5;
}
.ch-card__sub {
  display: block;
  font-size: 26rpx;
  color: #86909C;
  margin-bottom: 16rpx;
}
.ch-card__actions {
  display: flex;
  justify-content: flex-end;
  gap: 12rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #F2F3F5;
}
.ch-card__btn {
  font-size: 26rpx;
  color: #4F46E5;
  padding: 8rpx 24rpx;
  border: 1rpx solid #E0E7FF;
  border-radius: var(--radius-full);
  font-weight: 500;
}
.ch-card__btn--danger {
  color: #E34D59;
  border-color: #FDECEE;
}
