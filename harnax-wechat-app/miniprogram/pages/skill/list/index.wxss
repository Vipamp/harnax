/* ---- Skill List — Modern Minimal ---- */
.s-card {
  background: #FFFFFF;
  border-radius: var(--radius-lg);
  padding: 28rpx;
  margin-bottom: 24rpx;
  box-shadow: var(--shadow);
  border: 1rpx solid var(--border-color);
}
.s-card:active { background: #F7F8FA; }
.s-card__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12rpx;
}
.s-card__name {
  font-size: 32rpx;
  font-weight: 600;
  color: #1D2129;
}
.s-card__desc {
  display: block;
  font-size: 26rpx;
  color: #86909C;
  line-height: 1.6;
  margin-bottom: 10rpx;
}
.s-card__sub {
  display: block;
  font-size: 24rpx;
  color: #86909C;
  margin-bottom: 16rpx;
  word-break: break-all;
}
.s-card__actions {
  display: flex;
  justify-content: flex-end;
  gap: 12rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid #F2F3F5;
}
.s-card__btn {
  font-size: 26rpx;
  color: #4F46E5;
  padding: 8rpx 24rpx;
  border: 1rpx solid #E0E7FF;
  border-radius: var(--radius-full);
  font-weight: 500;
}
.s-card__btn--danger {
  color: #E34D59;
  border-color: #FDECEE;
}
