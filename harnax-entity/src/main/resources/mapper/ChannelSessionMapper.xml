<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.ChannelSessionMapper">

    <!-- Result Map -->
    <resultMap id="ChannelSessionResultMap" type="com.agnetix.harnax.entity.ChannelSession">
        <id     column="id"                 property="id"/>
        <result column="tenant_id"          property="tenantId"/>
        <result column="name"               property="name"/>
        <result column="type"               property="type"/>
        <result column="agent_id"           property="agentId"/>
        <result column="callback_key"       property="callbackKey"/>
        <result column="session_id"         property="sessionId"/>
        <result column="communication_mode" property="communicationMode"/>
        <result column="enabled"            property="enabled"/>
        <result column="config_json"        property="configJson"/>
        <result column="description"        property="description"/>
        <result column="creator"            property="creator"/>
        <result column="status"             property="status"/>
        <result column="active"             property="active"/>
        <result column="create_time"        property="createTime"/>
        <result column="update_time"        property="updateTime"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, tenant_id, name, type, agent_id, callback_key, session_id,
        communication_mode, enabled, config_json, description,
        creator, status, active, create_time, update_time
    </sql>

    <!-- ==================== Basic CRUD Methods ==================== -->

    <select id="selectById" resultMap="ChannelSessionResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM channel_session
        WHERE id = #{id} AND active = 1
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO channel_session (
            tenant_id, name, type, agent_id, callback_key, session_id,
            communication_mode, enabled, config_json, description,
            creator, status, active, create_time, update_time
        ) VALUES (
            #{tenantId}, #{name}, #{type}, #{agentId}, #{callbackKey}, #{sessionId},
            #{communicationMode}, #{enabled}, #{configJson}, #{description},
            #{creator}, #{status}, #{active}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById">
        UPDATE channel_session SET
            tenant_id          = #{tenantId},
            name               = #{name},
            type               = #{type},
            agent_id           = #{agentId},
            callback_key       = #{callbackKey},
            session_id         = #{sessionId},
            communication_mode = #{communicationMode},
            enabled            = #{enabled},
            config_json        = #{configJson},
            description        = #{description},
            status             = #{status},
            active             = #{active},
            update_time        = #{updateTime}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE channel_session SET active = 0, update_time = NOW() WHERE id = #{id}
    </update>

    <!-- ==================== Custom Query Methods ==================== -->

    <select id="selectBySessionId" resultMap="ChannelSessionResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM channel_session
        WHERE session_id = #{sessionId} AND active = 1
        LIMIT 1
    </select>

    <select id="selectByCallbackKey" resultMap="ChannelSessionResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM channel_session
        WHERE callback_key = #{callbackKey} AND active = 1
        LIMIT 1
    </select>

</mapper>
