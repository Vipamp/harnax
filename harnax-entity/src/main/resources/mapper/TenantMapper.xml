<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.TenantMapper">

    <!-- Result Map -->
    <resultMap id="TenantResultMap" type="com.agnetix.harnax.entity.TenantEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="status" property="status"/>
        <result column="creator" property="creator"/>
        <result column="active" property="active"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- Query tenant by ID -->
    <select id="selectById" resultMap="TenantResultMap">
        SELECT * FROM tenant
        WHERE id = #{id}
        AND active = 1
        LIMIT 1
    </select>

    <!-- Query tenant by name -->
    <select id="selectByName" resultMap="TenantResultMap">
        SELECT * FROM tenant
        WHERE name = #{name}
        AND active = 1
        LIMIT 1
    </select>

    <!-- Query tenant list -->
    <select id="selectList" resultMap="TenantResultMap">
        SELECT * FROM tenant
        WHERE active = 1
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- Insert tenant -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tenant (
            name,
            status,
            creator,
            active,
            create_time,
            update_time
        ) VALUES (
            #{name},
            #{status},
            #{creator},
            #{active},
            #{createTime},
            #{updateTime}
        )
    </insert>

    <!-- Update tenant by ID -->
    <update id="updateById">
        UPDATE tenant
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
        AND active = 1
    </update>

    <!-- Update tenant status -->
    <update id="updateStatus">
        UPDATE tenant
        SET status = #{status},
            update_time = NOW()
        WHERE id = #{id}
        AND active = 1
    </update>

    <!-- Delete tenant by ID (logical delete) -->
    <update id="deleteById">
        UPDATE tenant
        SET active = 0,
            update_time = NOW()
        WHERE id = #{id}
        AND active = 1
    </update>

</mapper>
