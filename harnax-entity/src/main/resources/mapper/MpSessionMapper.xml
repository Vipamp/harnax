<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.MpSessionMapper">

    <!-- Result Map -->
    <resultMap id="MpSessionResultMap" type="com.agnetix.harnax.entity.MpSession">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="session_name" property="sessionName"/>
        <result column="router_session_id" property="routerSessionId"/>
        <result column="agent_id" property="agentId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- selectById -->
    <select id="selectById" resultMap="MpSessionResultMap">
        SELECT * FROM mp_session WHERE id = #{id} AND status = 1 LIMIT 1
    </select>

    <!-- selectByUserId -->
    <select id="selectByUserId" resultMap="MpSessionResultMap">
        SELECT * FROM mp_session
        WHERE user_id = #{userId} AND status = 1
        ORDER BY update_time DESC
    </select>

    <!-- selectByIdAndUserId -->
    <select id="selectByIdAndUserId" resultMap="MpSessionResultMap">
        SELECT * FROM mp_session WHERE id = #{id} AND user_id = #{userId} AND status = 1 LIMIT 1
    </select>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mp_session (user_id, session_name, router_session_id, agent_id, status, create_time, update_time)
        VALUES (#{userId}, #{sessionName}, #{routerSessionId}, #{agentId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE mp_session SET
            session_name = #{sessionName},
            update_time = #{updateTime}
        WHERE id = #{id} AND status = 1
    </update>

    <!-- deleteById (soft delete: set status = 0) -->
    <update id="deleteById">
        UPDATE mp_session SET status = 0, update_time = NOW() WHERE id = #{id}
    </update>

    <!-- countByUserIdAndAgentId -->
    <select id="countByUserIdAndAgentId" resultType="int">
        SELECT COUNT(*) FROM mp_session
        WHERE user_id = #{userId} AND agent_id = #{agentId} AND status = 1
    </select>

    <!-- selectByRouterSessionId -->
    <select id="selectByRouterSessionId" resultMap="MpSessionResultMap">
        SELECT * FROM mp_session WHERE router_session_id = #{routerSessionId} AND status = 1 LIMIT 1
    </select>

</mapper>
