<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.PlanNoteMapper">

    <!-- Result Map -->
    <resultMap id="PlanNoteEntityResultMap" type="com.agnetix.harnax.entity.PlanNoteEntity">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="plan_id" property="planId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="expected_outcome" property="expectedOutcome"/>
        <result column="subtasks" property="subtasks"/>
        <result column="created_at" property="createdAt"/>
        <result column="finished_at" property="finishedAt"/>
        <result column="cost_timeseconds" property="costTimeseconds"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- ==================== Basic CRUD Methods ==================== -->

    <!-- selectById -->
    <select id="selectById" resultMap="PlanNoteEntityResultMap">
        SELECT * FROM plan_note WHERE id = #{id} LIMIT 1
    </select>

    <!-- deleteById -->
    <update id="deleteById">
        UPDATE plan_note SET active = 0 WHERE id = #{id}
    </update>

    <!-- selectBySessionIdAndPlanId -->
    <select id="selectBySessionIdAndPlanId" resultMap="PlanNoteEntityResultMap">
        SELECT * FROM plan_note WHERE session_id = #{sessionId} AND plan_id = #{planId} LIMIT 1
    </select>

    <!-- selectBySessionId -->
    <select id="selectBySessionId" resultMap="PlanNoteEntityResultMap">
        SELECT * FROM plan_note WHERE session_id = #{sessionId} ORDER BY created_at DESC
    </select>

    <!-- deleteBySessionId -->
    <delete id="deleteBySessionId">
        DELETE FROM plan_note WHERE session_id = #{sessionId}
    </delete>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO plan_note (
        session_id, plan_id, name, description, expected_outcome, subtasks, created_at, finished_at, cost_timeseconds, status
        ) VALUES (
        #{sessionId}, #{planId}, #{name}, #{description}, #{expectedOutcome}, #{subtasks}, #{createdAt}, #{finishedAt},
        #{costTimeseconds}, #{status}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE plan_note SET
        session_id = #{sessionId},
        plan_id = #{planId},
        name = #{name},
        description = #{description},
        expected_outcome = #{expectedOutcome},
        subtasks = #{subtasks},
        created_at = #{createdAt},
        finished_at = #{finishedAt},
        cost_timeseconds = #{costTimeseconds},
        status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
