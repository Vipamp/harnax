<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.McpUserCredentialMapper">

    <resultMap id="McpUserCredentialResultMap" type="com.agnetix.harnax.entity.McpUserCredential">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="mcp_id" property="mcpId"/>
        <result column="access_token_enc" property="accessTokenEnc"/>
        <result column="refresh_token_enc" property="refreshTokenEnc"/>
        <result column="access_expires_at" property="accessExpiresAt"/>
        <result column="scopes" property="scopes"/>
        <result column="status" property="status"/>
        <result column="last_error" property="lastError"/>
        <result column="last_refreshed_at" property="lastRefreshedAt"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- selectByUserAndMcp -->
    <select id="selectByUserAndMcp" resultMap="McpUserCredentialResultMap">
        SELECT * FROM mcp_user_credential
        WHERE tenant_id = #{tenantId} AND user_id = #{userId} AND mcp_id = #{mcpId}
        LIMIT 1
    </select>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mcp_user_credential (
        tenant_id, user_id, mcp_id, access_token_enc, refresh_token_enc, access_expires_at,
        scopes, status, last_error, last_refreshed_at, create_time, update_time
        ) VALUES (
        #{tenantId}, #{userId}, #{mcpId}, #{accessTokenEnc}, #{refreshTokenEnc}, #{accessExpiresAt},
        #{scopes}, #{status}, #{lastError}, #{lastRefreshedAt}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- updateById: unconditional SET so a revoke can null out both ciphertexts -->
    <update id="updateById">
        UPDATE mcp_user_credential SET
        access_token_enc = #{accessTokenEnc},
        refresh_token_enc = #{refreshTokenEnc},
        access_expires_at = #{accessExpiresAt},
        scopes = #{scopes},
        status = #{status},
        last_error = #{lastError},
        last_refreshed_at = #{lastRefreshedAt},
        update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- deleteByMcpId -->
    <delete id="deleteByMcpId">
        DELETE FROM mcp_user_credential WHERE mcp_id = #{mcpId}
    </delete>

</mapper>
