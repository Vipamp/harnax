<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.AgentTaskExecutionMapper">

    <resultMap id="AgentTaskExecutionResultMap" type="com.agnetix.harnax.entity.AgentTaskExecution">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="trigger_time" property="triggerTime"/>
        <result column="instance_id" property="instanceId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_task_execution (
            task_id, trigger_time, instance_id, start_time, end_time, status, create_time
        ) VALUES (
            #{taskId}, #{triggerTime}, #{instanceId}, #{startTime}, #{endTime}, #{status}, #{createTime}
        )
    </insert>

    <update id="updateStatus">
        UPDATE agent_task_execution
        SET status = #{status}, start_time = #{startTime}, end_time = #{endTime}
        WHERE id = #{id}
    </update>

    <select id="selectByTaskIdAndTriggerTime" resultMap="AgentTaskExecutionResultMap">
        SELECT * FROM agent_task_execution
        WHERE task_id = #{taskId} AND trigger_time = #{triggerTime}
        LIMIT 1
    </select>

    <delete id="deleteOldExecutions">
        DELETE FROM agent_task_execution WHERE create_time &lt; #{beforeTime}
    </delete>

</mapper>
