<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.ProcessLogMapper">

    <!-- Result Map -->
    <resultMap id="ProcessLogEntityResultMap" type="com.agnetix.harnax.entity.ProcessLogEntity">
        <id column="id" property="id"/>
        <result column="agent_id" property="agentId"/>
        <result column="agent_name" property="agentName"/>
        <result column="session_id" property="sessionId"/>
        <result column="message" property="message"/>
        <result column="log_type" property="logType"/>
        <result column="stack_trace" property="stackTrace"/>
        <result column="ts" property="ts"/>
    </resultMap>

    <!-- ==================== Basic CRUD Methods ==================== -->

    <!-- selectById -->
    <select id="selectById" resultMap="ProcessLogEntityResultMap">
        SELECT * FROM process_log WHERE id = #{id} LIMIT 1
    </select>

    <!-- deleteById -->
    <update id="deleteById">
        UPDATE process_log SET active = 0 WHERE id = #{id}
    </update>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO process_log (
        agent_id, agent_name, session_id, message, log_type, stack_trace, ts
        ) VALUES (
        #{agentId}, #{agentName}, #{sessionId}, #{message}, #{logType}, #{stackTrace}, #{ts}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE process_log SET
        agent_id = #{agentId},
        agent_name = #{agentName},
        session_id = #{sessionId},
        message = #{message},
        log_type = #{logType},
        stack_trace = #{stackTrace},
        ts = #{ts}
        WHERE id = #{id}
    </update>

</mapper>
