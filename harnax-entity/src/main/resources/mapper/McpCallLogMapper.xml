<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.McpCallLogMapper">

    <!-- Append-only. There is deliberately no update or delete statement: an audit table that the
         application can rewrite is not an audit table. -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mcp_call_log (
        tenant_id, user_id, mcp_id, session_id, tool_name, action, outcome, latency_ms, create_time
        ) VALUES (
        #{tenantId}, #{userId}, #{mcpId}, #{sessionId}, #{toolName}, #{action}, #{outcome}, #{latencyMs}, #{createTime}
        )
    </insert>

</mapper>
