<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.admin.mapper.SessionMapper">

    <!-- Result Map -->
    <resultMap id="SessionResultMap" type="com.agnetix.harnax.admin.entity.Session">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="session_description" property="sessionDescription"/>
        <result column="session_id" property="sessionId"/>
        <result column="agent_id" property="agentId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="system_prompt" property="systemPrompt"/>
        <result column="model_id" property="modelId"/>
        <result column="enable_think" property="enableThink"/>
        <result column="enable_search" property="enableSearch"/>
        <result column="enable_plan" property="enablePlan"/>
        <result column="mcp_list" property="mcpList"/>
        <result column="skill_list" property="skillList"/>
        <result column="owner" property="owner"/>
        <result column="status" property="status"/>
        <result column="is_public" property="isPublic"/>
        <result column="creator" property="creator"/>
        <result column="active" property="active"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- ==================== Basic CRUD Methods ==================== -->

    <!-- selectById -->
    <select id="selectById" resultMap="SessionResultMap">
        SELECT * FROM session WHERE id = #{id} AND active = 1 LIMIT 1
    </select>

    <!-- deleteById -->
    <update id="deleteById">
        UPDATE session SET active = 0 WHERE id = #{id}
    </update>

    <!-- countByTitle -->
    <select id="countByTitle" resultType="int">
        SELECT COUNT(*) FROM session WHERE title = #{title} AND active = 1
    </select>

    <!-- updateStatus -->
    <update id="updateStatus">
        UPDATE session SET status = #{status}, update_time = NOW() WHERE id = #{id} AND active = 1
    </update>

    <!-- selectBySessionIdAndStatus -->
    <select id="selectBySessionIdAndStatus" resultMap="SessionResultMap">
        SELECT * FROM session WHERE session_id = #{sessionId} AND status = #{status} LIMIT 1
    </select>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO session (
            session_id, title, session_description, agent_id,
            name, description, system_prompt, model_id,
            enable_think, enable_search, enable_plan,
            mcp_list, skill_list, owner,
            status, is_public, creator, active, create_time, update_time
        ) VALUES (
            #{sessionId}, #{title}, #{sessionDescription}, #{agentId},
            #{name}, #{description}, #{systemPrompt}, #{modelId},
            #{enableThink}, #{enableSearch}, #{enablePlan},
            #{mcpList}, #{skillList}, #{owner},
            #{status}, #{isPublic}, #{creator}, #{active}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE session SET
        title = #{title},
        session_description = #{sessionDescription},
        agent_id = #{agentId},
        model_id = #{modelId},
        enable_think = #{enableThink},
        enable_search = #{enableSearch},
        enable_plan = #{enablePlan},
        system_prompt = #{systemPrompt},
        is_public = #{isPublic},
        active = #{active},
        update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- selectSessionList -->
    <select id="selectSessionList" resultMap="SessionResultMap">
        SELECT * FROM session
        WHERE active = 1
        AND (is_public = 1 OR creator = #{currentUsername})
        <if test='keyword != null and keyword != ""'>
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test='status != null'>
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- selectByAgentId -->
    <select id="selectByAgentId" resultMap="SessionResultMap">
        SELECT * FROM session
        WHERE agent_id = #{agentId} AND active = 1
        ORDER BY create_time DESC
        LIMIT 10
    </select>

</mapper>
