<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.EnvVariableMapper">

    <resultMap id="EnvVariableResultMap" type="com.agnetix.harnax.entity.EnvVariable">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="env_key" property="envKey"/>
        <result column="env_value" property="envValue"/>
        <result column="description" property="description"/>
        <result column="sensitive" property="sensitive"/>
        <result column="enabled" property="enabled"/>
        <result column="creator" property="creator"/>
        <result column="active" property="active"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="EnvVariableResultMap">
        SELECT * FROM env_variable WHERE id = #{id} AND active = 1 LIMIT 1
    </select>

    <update id="deleteById">
        UPDATE env_variable SET active = 0, update_time = NOW() WHERE id = #{id} AND active = 1
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO env_variable (
            tenant_id, env_key, env_value, description, `sensitive`, enabled,
            creator, active, create_time, update_time
        ) VALUES (
            #{tenantId}, #{envKey}, #{envValue}, #{description}, #{sensitive}, #{enabled},
            #{creator}, #{active}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById">
        UPDATE env_variable SET
            env_key = #{envKey},
            env_value = #{envValue},
            description = #{description},
            `sensitive` = #{sensitive},
            enabled = #{enabled},
            update_time = #{updateTime}
        WHERE id = #{id} AND active = 1
    </update>

    <update id="toggleEnabled">
        UPDATE env_variable SET enabled = #{enabled}, update_time = NOW() WHERE id = #{id} AND active = 1
    </update>

    <select id="selectEnvVariableList" resultMap="EnvVariableResultMap">
        SELECT * FROM env_variable
        WHERE active = 1
        AND creator = #{currentUsername}
        <if test='keyword != null and keyword != ""'>
            AND (env_key LIKE CONCAT('%', #{keyword}, '%')
            OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY update_time DESC
    </select>

</mapper>
