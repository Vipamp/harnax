<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agnetix.harnax.mapper.SysTokenBlacklistMapper">

    <!-- Result Map -->
    <resultMap id="SysTokenBlacklistResultMap" type="com.agnetix.harnax.entity.SysTokenBlacklist">
        <id column="id" property="id"/>
        <result column="token" property="token"/>
        <result column="token_hash" property="tokenHash"/>
        <result column="username" property="username"/>
        <result column="user_id" property="userId"/>
        <result column="reason" property="reason"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="create_ip" property="createIp"/>
    </resultMap>

    <!-- ==================== Basic CRUD Methods ==================== -->

    <!-- selectById -->
    <select id="selectById" resultMap="SysTokenBlacklistResultMap">
        SELECT * FROM sys_token_blacklist WHERE id = #{id} LIMIT 1
    </select>

    <!-- deleteById: this table has no `active` column (a revoked token entry is not a business
         record worth keeping), so removal is a real DELETE, matching the production DDL -->
    <delete id="deleteById">
        DELETE FROM sys_token_blacklist WHERE id = #{id}
    </delete>

    <!-- selectByTokenHash -->
    <select id="selectByTokenHash" resultMap="SysTokenBlacklistResultMap">
        SELECT * FROM sys_token_blacklist WHERE token_hash = #{tokenHash} AND expire_time > NOW()
    </select>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_token_blacklist (
        token, token_hash, username, user_id, reason, expire_time, create_time, create_ip
        ) VALUES (
        #{token}, #{tokenHash}, #{username}, #{userId}, #{reason}, #{expireTime}, #{createTime}, #{createIp}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE sys_token_blacklist SET
        token = #{token},
        token_hash = #{tokenHash},
        username = #{username},
        user_id = #{userId},
        reason = #{reason},
        expire_time = #{expireTime},
        create_ip = #{createIp}
        WHERE id = #{id}
    </update>

</mapper>
