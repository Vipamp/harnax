<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vipamp.vipclaw.admin.mapper.UserTenantMapper">

    <!-- 结果映射 -->
    <resultMap id="UserTenantResultMap" type="com.vipamp.vipclaw.admin.entity.UserTenantEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="joined_at" property="joinedAt"/>
    </resultMap>

    <!-- 根据用户 ID 查询租户列表 -->
    <select id="selectByUserId" resultMap="UserTenantResultMap">
        SELECT * FROM user_tenant
        WHERE user_id = #{userId}
        AND status = 1
        ORDER BY joined_at DESC
    </select>

    <!-- 根据租户 ID 查询用户列表 -->
    <select id="selectByTenantId" resultMap="UserTenantResultMap">
        SELECT * FROM user_tenant
        WHERE tenant_id = #{tenantId}
        AND status = 1
        ORDER BY joined_at DESC
    </select>

    <!-- 根据用户 ID 和租户 ID 查询 -->
    <select id="selectByUserIdAndTenantId" resultMap="UserTenantResultMap">
        SELECT * FROM user_tenant
        WHERE user_id = #{userId}
        AND tenant_id = #{tenantId}
        LIMIT 1
    </select>

    <!-- 插入用户-租户关联 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_tenant (
            user_id,
            tenant_id,
            role,
            status,
            joined_at
        ) VALUES (
            #{userId},
            #{tenantId},
            #{role},
            #{status},
            #{joinedAt}
        )
    </insert>

    <!-- 根据用户 ID 和租户 ID 删除 -->
    <update id="deleteByUserIdAndTenantId">
        UPDATE user_tenant
        SET status = 0
        WHERE user_id = #{userId}
        AND tenant_id = #{tenantId}
        AND status = 1
    </update>

    <!-- 根据租户 ID 删除所有关联 -->
    <update id="deleteByTenantId">
        UPDATE user_tenant
        SET status = 0
        WHERE tenant_id = #{tenantId}
        AND status = 1
    </update>

    <!-- 更新用户角色 -->
    <update id="updateRole">
        UPDATE user_tenant
        SET role = #{role}
        WHERE user_id = #{userId}
        AND tenant_id = #{tenantId}
        AND status = 1
    </update>

</mapper>
