<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vipamp.vipclaw.admin.mapper.SysJobLogMapper">

    <!-- 结果映射 -->
    <resultMap id="SysJobLogResultMap" type="com.vipamp.vipclaw.admin.entity.SysJobLog">
        <id column="id" property="id"/>
        <result column="job_id" property="jobId"/>
        <result column="job_name" property="jobName"/>
        <result column="job_group" property="jobGroup"/>
        <result column="invoke_target" property="invokeTarget"/>
        <result column="job_message" property="jobMessage"/>
        <result column="status" property="status"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="creator" property="creator"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- ==================== 基础 CRUD 方法 ==================== -->

    <!-- selectById -->
    <select id="selectById" resultMap="SysJobLogResultMap">
        SELECT * FROM sys_job_log WHERE id = #{id} LIMIT 1
    </select>

    <!-- deleteById -->
    <update id="deleteById">
        UPDATE sys_job_log SET active = 0 WHERE id = #{id}
    </update>

    <!-- insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_job_log (
        job_id, job_name, job_group, invoke_target, job_message, status, start_time, end_time, creator, create_time
        ) VALUES (
        #{jobId}, #{jobName}, #{jobGroup}, #{invokeTarget}, #{jobMessage}, #{status}, #{startTime}, #{endTime},
        #{creator}, #{createTime}
        )
    </insert>

    <!-- updateById -->
    <update id="updateById">
        UPDATE sys_job_log SET
        job_id = #{jobId},
        job_name = #{jobName},
        job_group = #{jobGroup},
        invoke_target = #{invokeTarget},
        job_message = #{jobMessage},
        status = #{status},
        start_time = #{startTime},
        end_time = #{endTime},
        creator = #{creator}
        WHERE id = #{id}
    </update>

    <!-- selectJobLogList -->
    <select id="selectJobLogList" resultMap="SysJobLogResultMap">
        SELECT * FROM sys_job_log
        WHERE 1=1
        <if test='jobId != null'>
            AND job_id = #{jobId}
        </if>
        <if test='jobName != null and jobName != ""'>
            AND job_name LIKE CONCAT('%', #{jobName}, '%')
        </if>
        <if test='status != null'>
            AND status = #{status}
        </if>
        <if test='startTime != null'>
            AND create_time >= #{startTime}
        </if>
        <if test='endTime != null'>
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
