# 国际化支持实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 Harnax 项目建立完整的前后端国际化体系，实现错误消息、系统提示、业务文案根据用户语言偏好动态切换。

**架构：** 后端使用 Spring Boot MessageSource + AcceptLanguageLocaleResolver 解析语言偏好，前端通过 Axios 拦截器传递 Accept-Language 请求头，所有错误消息通过 MessageUtil 工具类获取国际化文案。

**技术栈：** Spring Boot 3 + Kotlin + MyBatis-Plus（后端），React + UmiJS + Ant Design Pro + react-intl（前端）

---

## 文件结构

### 新建文件

**后端：**
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/config/I18nConfig.kt` - 国际化配置类
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/i18n/MessageUtil.kt` - 消息工具类
- `harnax-admin/src/main/resources/i18n/messages.properties` - 默认消息（英文）
- `harnax-admin/src/main/resources/i18n/messages_zh_CN.properties` - 中文消息
- `harnax-admin/src/main/resources/i18n/messages_error.properties` - 错误消息（英文）
- `harnax-admin/src/main/resources/i18n/messages_error_zh_CN.properties` - 错误消息（中文）

**测试：**
- `harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/config/I18nConfigTest.kt` - 国际化配置测试
- `harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/i18n/MessageUtilTest.kt` - 消息工具类测试

### 修改文件

**后端：**
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/exception/BizException.kt` - 添加 MessageUtil 支持
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/exception/GlobalExceptionHandler.kt` - 更新错误处理
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/AuthServiceImpl.kt` - 迁移错误消息
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/interceptor/TenantInterceptor.kt` - 迁移错误消息
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/SysUserServiceImpl.kt` - 迁移错误消息
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/UserTenantServiceImpl.kt` - 迁移错误消息
- `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/TenantServiceImpl.kt` - 迁移错误消息

**前端：**
- `harnax-webui/src/requestErrorConfig.ts` - 添加 Accept-Language 请求头
- `harnax-webui/src/locales/zh-CN.ts` - 补充缺失文案
- `harnax-webui/src/locales/en-US.ts` - 补充缺失文案

---

## 任务 1：创建消息资源文件骨架

**文件：**
- 创建：`harnax-admin/src/main/resources/i18n/messages.properties`
- 创建：`harnax-admin/src/main/resources/i18n/messages_zh_CN.properties`
- 创建：`harnax-admin/src/main/resources/i18n/messages_error.properties`
- 创建：`harnax-admin/src/main/resources/i18n/messages_error_zh_CN.properties`

- [ ] **步骤 1：创建 i18n 目录和消息文件**

创建目录 `harnax-admin/src/main/resources/i18n/`

创建 `messages.properties`（英文默认）：
```properties
# Success messages
success.operation.completed=Operation completed successfully
success.user.created=User created successfully
success.user.updated=User updated successfully
success.user.deleted=User deleted successfully

# General messages
app.name=Harnax
app.version=1.0.0
```

创建 `messages_zh_CN.properties`（中文）：
```properties
# Success messages
success.operation.completed=操作成功完成
success.user.created=用户创建成功
success.user.updated=用户更新成功
success.user.deleted=用户删除成功

# General messages
app.name=Harnax
app.version=1.0.0
```

创建 `messages_error.properties`（错误消息-英文）：
```properties
# User errors
error.user.notfound=User not found
error.user.invalid_credentials=Invalid username or password
error.user.disabled=User has been disabled, please contact administrator
error.user.no_tenant=Your account has no available tenant, please contact administrator
error.user.username_exists=Username already exists
error.user.phone_exists=Phone number already exists
error.user.email_exists=Email already exists

# Captcha errors
error.captcha.required=Please enter captcha
error.captcha.key_required=Captcha key cannot be empty
error.captcha.invalid=Invalid captcha, please re-enter

# Tenant errors
error.tenant.invalid_id=Invalid tenant ID
error.tenant.access_denied=Access denied to this tenant
error.tenant.user_disabled=You have been disabled in this tenant
error.tenant.name_exists=Tenant name already exists
error.tenant.notfound=Tenant not found

# Auth errors
error.auth.not_logged_in=User not logged in

# Validation errors
error.validation.required={0} is required
error.validation.invalid={0} is invalid
error.validation.email_invalid=Invalid email format
error.validation.phone_invalid=Invalid phone number format
```

创建 `messages_error_zh_CN.properties`（错误消息-中文）：
```properties
# User errors
error.user.notfound=用户不存在
error.user.invalid_credentials=用户名或密码错误
error.user.disabled=用户已被禁用，请联系管理员
error.user.no_tenant=您的账号暂无可用租户，请联系管理员
error.user.username_exists=用户名已存在
error.user.phone_exists=手机号已存在
error.user.email_exists=邮箱已存在

# Captcha errors
error.captcha.required=请输入验证码
error.captcha.key_required=验证码 key 不能为空
error.captcha.invalid=验证码错误，请重新输入

# Tenant errors
error.tenant.invalid_id=无效的租户ID
error.tenant.access_denied=无权访问该租户
error.tenant.user_disabled=您在该租户下已被禁用
error.tenant.name_exists=租户名称已存在
error.tenant.notfound=租户不存在

# Auth errors
error.auth.not_logged_in=用户未登录

# Validation errors
error.validation.required={0}不能为空
error.validation.invalid={0}无效
error.validation.email_invalid=邮箱格式不正确
error.validation.phone_invalid=手机号格式不正确
```

- [ ] **步骤 2：验证文件创建成功**

运行：
```bash
ls -la harnax-admin/src/main/resources/i18n/
```

预期：显示 4 个 properties 文件

- [ ] **步骤 3：Commit**

```bash
git add harnax-admin/src/main/resources/i18n/
git commit -m "feat(i18n): 创建消息资源文件骨架

- 添加 messages.properties（英文默认）
- 添加 messages_zh_CN.properties（中文）
- 添加 messages_error.properties（错误消息-英文）
- 添加 messages_error_zh_CN.properties（错误消息-中文）
- 包含用户、租户、验证码、验证等模块的错误消息"
```

---

## 任务 2：创建 I18nConfig 配置类

**文件：**
- 创建：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/config/I18nConfig.kt`

- [ ] **步骤 1：编写 I18nConfig 配置类**

创建 `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/config/I18nConfig.kt`：

```kotlin
package com.agnetix.harnax.admin.config

import org.springframework.context.MessageSource
import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration
import org.springframework.context.support.ReloadableResourceBundleMessageSource
import org.springframework.web.servlet.LocaleResolver
import org.springframework.web.servlet.i18n.AcceptHeaderLocaleResolver
import java.util.Locale

/**
 * 国际化配置类
 * 
 * 配置 MessageSource 和 LocaleResolver
 */
@Configuration
class I18nConfig {

    /**
     * 配置消息源
     * 
     * 从 i18n/messages*.properties 加载消息资源
     */
    @Bean
    fun messageSource(): MessageSource {
        val messageSource = ReloadableResourceBundleMessageSource()
        messageSource.setBasename("classpath:i18n/messages")
        messageSource.setDefaultEncoding("UTF-8")
        messageSource.setCacheSeconds(3600) // 缓存 1 小时
        messageSource.setUseCodeAsDefaultMessage(true) // 找不到消息时返回 code 本身
        return messageSource
    }

    /**
     * 配置错误消息源
     * 
     * 从 i18n/messages_error*.properties 加载错误消息资源
     */
    @Bean
    fun errorMessageSource(): MessageSource {
        val messageSource = ReloadableResourceBundleMessageSource()
        messageSource.setBasename("classpath:i18n/messages_error")
        messageSource.setDefaultEncoding("UTF-8")
        messageSource.setCacheSeconds(3600)
        messageSource.setUseCodeAsDefaultMessage(true)
        return messageSource
    }

    /**
     * 配置语言解析器
     * 
     * 从 Accept-Language 请求头解析用户语言偏好
     * 默认使用英文
     */
    @Bean
    fun localeResolver(): LocaleResolver {
        val resolver = AcceptHeaderLocaleResolver()
        resolver.setDefaultLocale(Locale.ENGLISH)
        resolver.setSupportedLocales(listOf(
            Locale.ENGLISH,
            Locale.SIMPLIFIED_CHINESE,
            Locale("zh", "CN")
        ))
        return resolver
    }
}
```

- [ ] **步骤 2：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/config/I18nConfig.kt
git commit -m "feat(i18n): 添加国际化配置类

- 配置 MessageSource 加载 messages*.properties
- 配置 errorMessageSource 加载 messages_error*.properties
- 配置 AcceptHeaderLocaleResolver 解析 Accept-Language 请求头
- 设置默认语言为英文，支持中英文切换"
```

---

## 任务 3：创建 MessageUtil 工具类

**文件：**
- 创建：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/i18n/MessageUtil.kt`

- [ ] **步骤 1：创建 MessageUtil 工具类**

创建 `harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/i18n/MessageUtil.kt`：

```kotlin
package com.agnetix.harnax.admin.i18n

import org.springframework.context.MessageSource
import org.springframework.context.i18n.LocaleContextHolder
import org.springframework.stereotype.Component

/**
 * 国际化消息工具类
 * 
 * 封装 MessageSource 的调用，提供便捷的消息获取方法
 */
@Component
class MessageUtil(
    private val messageSource: MessageSource
) {
    /**
     * 获取国际化消息
     * 
     * @param code 消息代码
     * @param args 消息参数
     * @return 国际化消息文本
     */
    fun getMessage(code: String, vararg args: Any): String {
        val locale = LocaleContextHolder.getLocale()
        return try {
            messageSource.getMessage(code, args.toTypedArray(), locale)
        } catch (e: Exception) {
            // 如果找不到消息，返回 code 本身
            code
        }
    }

    /**
     * 获取国际化消息（带默认值）
     * 
     * @param code 消息代码
     * @param defaultMessage 默认消息
     * @param args 消息参数
     * @return 国际化消息文本
     */
    fun getMessage(code: String, defaultMessage: String, vararg args: Any): String {
        val locale = LocaleContextHolder.getLocale()
        return try {
            messageSource.getMessage(code, args.toTypedArray(), locale)
        } catch (e: Exception) {
            defaultMessage
        }
    }
}
```

- [ ] **步骤 2：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/i18n/MessageUtil.kt
git commit -m "feat(i18n): 添加消息工具类

- 创建 MessageUtil 封装 MessageSource 调用
- 支持参数化消息（如：User {0} not found）
- 提供带默认值的重载方法
- 找不到消息时返回 code 本身作为 fallback"
```

---

## 任务 4：编写 I18nConfig 单元测试

**文件：**
- 创建：`harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/config/I18nConfigTest.kt`

- [ ] **步骤 1：编写配置测试**

创建 `harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/config/I18nConfigTest.kt`：

```kotlin
package com.agnetix.harnax.admin.config

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.context.MessageSource
import org.springframework.web.servlet.LocaleResolver
import org.springframework.web.servlet.i18n.AcceptHeaderLocaleResolver
import java.util.Locale

@SpringBootTest
class I18nConfigTest {

    @Autowired
    lateinit var messageSource: MessageSource

    @Autowired
    lateinit var errorMessageSource: MessageSource

    @Autowired
    lateinit var localeResolver: LocaleResolver

    @Test
    fun `messageSource bean should be configured`() {
        assertNotNull(messageSource)
    }

    @Test
    fun `errorMessageSource bean should be configured`() {
        assertNotNull(errorMessageSource)
    }

    @Test
    fun `localeResolver should be AcceptHeaderLocaleResolver`() {
        assertIs<AcceptHeaderLocaleResolver>(localeResolver)
    }

    @Test
    fun `messageSource should load Chinese message`() {
        val message = messageSource.getMessage("error.user.notfound", arrayOf(), Locale.SIMPLIFIED_CHINESE)
        assertEquals("用户不存在", message)
    }

    @Test
    fun `messageSource should load English message`() {
        val message = messageSource.getMessage("error.user.notfound", arrayOf(), Locale.ENGLISH)
        assertEquals("User not found", message)
    }

    @Test
    fun `messageSource should support parameterized messages`() {
        val message = messageSource.getMessage("error.validation.required", arrayOf("用户名"), Locale.SIMPLIFIED_CHINESE)
        assertEquals("用户名不能为空", message)
    }

    @Test
    fun `messageSource should return code when message not found`() {
        val message = messageSource.getMessage("non.existent.code", arrayOf(), Locale.ENGLISH)
        assertEquals("non.existent.code", message)
    }
}
```

- [ ] **步骤 2：运行测试验证通过**

运行：
```bash
cd harnax-admin && mvn test -Dtest=I18nConfigTest
```

预期：所有测试 PASS

- [ ] **步骤 3：Commit**

```bash
git add harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/config/I18nConfigTest.kt
git commit -m "test(i18n): 添加国际化配置单元测试

- 验证 MessageSource Bean 正确配置
- 验证 LocaleResolver 使用 AcceptHeaderLocaleResolver
- 验证中英文消息正确加载
- 验证参数化消息支持
- 验证找不到消息时返回 code 本身"
```

---

## 任务 5：编写 MessageUtil 单元测试

**文件：**
- 创建：`harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/i18n/MessageUtilTest.kt`

- [ ] **步骤 1：编写 MessageUtil 测试**

创建 `harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/i18n/MessageUtilTest.kt`：

```kotlin
package com.agnetix.harnax.admin.i18n

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.test.context.ContextConfiguration
import java.util.Locale
import org.springframework.test.context.TestPropertySource

@SpringBootTest
@TestPropertySource(properties = [
    "spring.messages.basename=i18n/messages,i18n/messages_error",
    "spring.messages.encoding=UTF-8"
])
class MessageUtilTest {

    @Autowired
    lateinit var messageUtil: MessageUtil

    @Test
    fun `getMessage should return Chinese message`() {
        Locale.setDefault(Locale.SIMPLIFIED_CHINESE)
        val message = messageUtil.getMessage("error.user.notfound")
        assertEquals("用户不存在", message)
    }

    @Test
    fun `getMessage should return English message`() {
        Locale.setDefault(Locale.ENGLISH)
        val message = messageUtil.getMessage("error.user.notfound")
        assertEquals("User not found", message)
    }

    @Test
    fun `getMessage should support parameters`() {
        Locale.setDefault(Locale.SIMPLIFIED_CHINESE)
        val message = messageUtil.getMessage("error.validation.required", "用户名")
        assertEquals("用户名不能为空", message)
    }

    @Test
    fun `getMessage should return code when not found`() {
        Locale.setDefault(Locale.ENGLISH)
        val message = messageUtil.getMessage("non.existent.code")
        assertEquals("non.existent.code", message)
    }

    @Test
    fun `getMessage with default should return default when not found`() {
        Locale.setDefault(Locale.ENGLISH)
        val message = messageUtil.getMessage("non.existent.code", "Default message")
        assertEquals("Default message", message)
    }
}
```

- [ ] **步骤 2：运行测试验证通过**

运行：
```bash
cd harnax-admin && mvn test -Dtest=MessageUtilTest
```

预期：所有测试 PASS

- [ ] **步骤 3：Commit**

```bash
git add harnax-admin/src/test/kotlin/com/agnetix/harnax/admin/i18n/MessageUtilTest.kt
git commit -m "test(i18n): 添加 MessageUtil 单元测试

- 验证中英文消息获取
- 验证参数化消息支持
- 验证找不到消息时返回 code
- 验证默认值 fallback 机制"
```

---

## 任务 6：前端添加 Accept-Language 请求头

**文件：**
- 修改：`harnax-webui/src/requestErrorConfig.ts:36-65`

- [ ] **步骤 1：修改请求拦截器**

修改 `harnax-webui/src/requestErrorConfig.ts`，在现有 token 拦截器后添加语言同步：

```typescript
// 请求拦截器 - 添加 JWT Token、X-Tenant-ID 和 Accept-Language
requestInterceptors: [
async (url, options) => {
  // 从 localStorage 获取 token
  const tokenInfoStr = localStorage.getItem('tokenInfo');
  
  let headers: Record<string, string> = {
    ...options.headers,
  };

  if (tokenInfoStr) {
    try {
      const tokenInfo = JSON.parse(tokenInfoStr);
      
      if (tokenInfo.accessToken) {
        // 在请求头中添加 Authorization
        headers['Authorization'] = `Bearer ${tokenInfo.accessToken}`;
        
        // 添加 X-Tenant-ID 请求头（如果存在当前租户ID）
        if (tokenInfo.currentUser?.currentTenantId) {
          headers['X-Tenant-ID'] = String(tokenInfo.currentUser.currentTenantId);
        }
      }
    } catch (e) {
      console.error('[请求拦截器] 解析 token 失败:', e);
    }
  }
  
  // 添加 Accept-Language 请求头
  const locale = localStorage.getItem('umi_locale') || 'zh-CN';
  headers['Accept-Language'] = locale;
  
  return { url, options: { ...options, headers } };
},
],
```

- [ ] **步骤 2：验证修改**

打开浏览器开发者工具，发起任意 API 请求，检查请求头是否包含 `Accept-Language: zh-CN` 或 `Accept-Language: en-US`

- [ ] **步骤 3：Commit**

```bash
git add harnax-webui/src/requestErrorConfig.ts
git commit -m "feat(i18n): 前端添加 Accept-Language 请求头

- 在 Axios 请求拦截器中读取 localStorage 的 umi_locale
- 添加 Accept-Language 请求头到所有 API 请求
- 默认使用 zh-CN 作为语言"
```

---

## 任务 7：迁移 AuthServiceImpl 错误消息

**文件：**
- 修改：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/AuthServiceImpl.kt`

- [ ] **步骤 1：注入 MessageUtil**

修改 `AuthServiceImpl.kt`，在构造函数中添加 MessageUtil：

```kotlin
class AuthServiceImpl(
    private val sysUserService: SysUserService,
    private val captchaService: CaptchaService,
    private val jwtUtil: JwtUtil,
    private val tokenBlacklistService: SysTokenBlacklistService,
    private val sysUserMapper: SysUserMapper,
    private val userTenantService: UserTenantService,
    private val editionUtil: EditionUtil,
    private val tenantMapper: TenantMapper,
    private val messageUtil: MessageUtil  // 新增
) : AuthService {
```

- [ ] **步骤 2：替换硬编码错误消息**

替换 AuthServiceImpl 中的所有 BizException：

```kotlin
// 第 50 行
val user: SysUser = sysUserService.getByUsername(request.username) 
    ?: throw BizException(messageUtil.getMessage("error.user.notfound"))

// 第 55 行
throw BizException(messageUtil.getMessage("error.user.invalid_credentials"))

// 第 62 行
throw BizException(messageUtil.getMessage("error.captcha.required"))

// 第 65 行
throw BizException(messageUtil.getMessage("error.captcha.key_required"))

// 第 68 行
throw BizException(messageUtil.getMessage("error.captcha.invalid"))

// 第 73 行
throw BizException(messageUtil.getMessage("error.user.disabled"))

// 第 98 行
throw BizException(messageUtil.getMessage("error.user.no_tenant"))
```

- [ ] **步骤 3：添加 import**

在文件顶部添加：
```kotlin
import com.agnetix.harnax.admin.i18n.MessageUtil
```

- [ ] **步骤 4：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/AuthServiceImpl.kt
git commit -m "refactor(i18n): 迁移 AuthServiceImpl 错误消息

- 注入 MessageUtil 工具类
- 替换 7 处硬编码错误消息为国际化消息
- 包括登录、验证码、用户状态等错误"
```

---

## 任务 8：迁移 TenantInterceptor 错误消息

**文件：**
- 修改：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/interceptor/TenantInterceptor.kt`

- [ ] **步骤 1：注入 MessageUtil 并替换错误消息**

修改 `TenantInterceptor.kt`：

```kotlin
package com.agnetix.harnax.admin.interceptor

import com.agnetix.harnax.admin.exception.BizException
import com.agnetix.harnax.admin.context.TenantContext
import com.agnetix.harnax.admin.mapper.UserTenantMapper
import com.agnetix.harnax.admin.security.SecurityUtils
import com.agnetix.harnax.admin.i18n.MessageUtil
import org.springframework.stereotype.Component
import org.springframework.web.servlet.HandlerInterceptor
import javax.servlet.http.HttpServletRequest
import javax.servlet.http.HttpServletResponse

@Component
class TenantInterceptor(
    private val userTenantMapper: UserTenantMapper,
    private val messageUtil: MessageUtil  // 新增
) : HandlerInterceptor {

    override fun preHandle(
        request: HttpServletRequest,
        response: HttpServletResponse,
        handler: Any
    ): Boolean {
        // 第 34 行
        val tenantId = request.getHeader("X-Tenant-ID")?.toLongOrNull()
            ?: throw BizException(messageUtil.getMessage("error.tenant.invalid_id"))

        // 第 37 行
        val currentUser = SecurityUtils.getCurrentUser()
            ?: throw BizException(messageUtil.getMessage("error.auth.not_logged_in"))

        // 验证用户是否属于该租户
        val userTenant = userTenantMapper.selectByUserIdAndTenantId(currentUser.id, tenantId)

        if (userTenant == null) {
            // 第 49 行
            throw BizException(messageUtil.getMessage("error.tenant.access_denied"))
        }

        if (userTenant.status == 0) {
            // 第 53 行
            throw BizException(messageUtil.getMessage("error.tenant.user_disabled"))
        }

        TenantContext.setTenantId(tenantId)
        return true
    }

    override fun afterCompletion(
        request: HttpServletRequest,
        response: HttpServletResponse,
        handler: Any,
        ex: Exception?
    ) {
        TenantContext.clear()
    }
}
```

- [ ] **步骤 2：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/interceptor/TenantInterceptor.kt
git commit -m "refactor(i18n): 迁移 TenantInterceptor 错误消息

- 注入 MessageUtil 工具类
- 替换 4 处硬编码错误消息为国际化消息
- 包括租户ID、登录状态、权限验证等错误"
```

---

## 任务 9：迁移 SysUserServiceImpl 错误消息

**文件：**
- 修改：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/SysUserServiceImpl.kt`

- [ ] **步骤 1：查找并读取 SysUserServiceImpl**

运行：
```bash
find harnax-admin/src/main/kotlin -name "SysUserServiceImpl.kt"
```

- [ ] **步骤 2：注入 MessageUtil 并替换错误消息**

参照任务 7 和 8 的模式，在 SysUserServiceImpl 中：
1. 添加 MessageUtil 到构造函数
2. 替换所有 `throw BizException("硬编码消息")` 为 `throw BizException(messageUtil.getMessage("error.xxx"))`

需要替换的错误消息：
- `用户名已存在` → `error.user.username_exists`
- `手机号已存在` → `error.user.phone_exists`
- `邮箱已存在` → `error.user.email_exists`
- `用户不存在` → `error.user.notfound`
- 等其他错误消息

- [ ] **步骤 3：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/SysUserServiceImpl.kt
git commit -m "refactor(i18n): 迁移 SysUserServiceImpl 错误消息

- 注入 MessageUtil 工具类
- 替换所有硬编码错误消息为国际化消息
- 包括用户名、手机号、邮箱重复等错误"
```

---

## 任务 10：迁移其他 Service 错误消息

**文件：**
- 修改：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/UserTenantServiceImpl.kt`
- 修改：`harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/TenantServiceImpl.kt`

- [ ] **步骤 1：迁移 UserTenantServiceImpl**

参照任务 7-9 的模式，迁移 UserTenantServiceImpl 中的错误消息：
- `用户不在该租户中` → `error.user.not_in_tenant`
- `租户不存在` → `error.tenant.notfound`
- `用户不存在` → `error.user.notfound`
- `用户已在该租户中` → `error.user.already_in_tenant`

- [ ] **步骤 2：迁移 TenantServiceImpl**

迁移 TenantServiceImpl 中的错误消息：
- `租户名称已存在` → `error.tenant.name_exists`
- `用户不存在` → `error.user.notfound`
- `租户不存在` → `error.tenant.notfound`
- `用户已在该租户中` → `error.user.already_in_tenant`

- [ ] **步骤 3：Commit**

```bash
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/UserTenantServiceImpl.kt
git add harnax-admin/src/main/kotlin/com/agnetix/harnax/admin/service/impl/TenantServiceImpl.kt
git commit -m "refactor(i18n): 迁移 UserTenantServiceImpl 和 TenantServiceImpl 错误消息

- 注入 MessageUtil 工具类
- 替换所有硬编码错误消息为国际化消息
- 包括租户管理、用户租户关联等错误"
```

---

## 任务 11：补充前端国际化文案

**文件：**
- 修改：`harnax-webui/src/locales/zh-CN.ts`
- 修改：`harnax-webui/src/locales/en-US.ts`

- [ ] **步骤 1：对比中英文文案**

运行：
```bash
cd harnax-webui/src/locales
diff zh-CN.ts en-US.ts | head -100
```

识别缺失的翻译项。

- [ ] **步骤 2：补充缺失文案**

对比两个文件，补充所有缺失的翻译项，确保 zh-CN 和 en-US 的 key 完全一致。

- [ ] **步骤 3：Commit**

```bash
git add harnax-webui/src/locales/zh-CN.ts
git add harnax-webui/src/locales/en-US.ts
git commit -m "feat(i18n): 补充前端国际化文案

- 对比 zh-CN 和 en-US 文件
- 补充所有缺失的翻译项
- 确保中英文文案完全对应"
```

---

## 任务 12：端到端测试

**文件：**
- 无修改，手动测试

- [ ] **步骤 1：测试中文语言流程**

1. 设置语言为中文：在前端切换语言到"简体中文"
2. 登录页面：验证所有文案显示中文
3. 输入错误密码：验证错误提示显示中文（如"用户名或密码错误"）
4. 用户管理页面：验证界面文案全部是中文
5. 创建用户时重复用户名：验证错误提示显示中文

- [ ] **步骤 2：测试英文语言流程**

1. 设置语言为英文：在前端切换语言到"English"
2. 刷新页面
3. 登录页面：验证所有文案显示英文
4. 输入错误密码：验证错误提示显示英文（如"Invalid username or password"）
5. 用户管理页面：验证界面文案全部是英文
6. 创建用户时重复用户名：验证错误提示显示英文

- [ ] **步骤 3：验证请求头**

打开浏览器开发者工具 → Network 标签，检查 API 请求的 Request Headers：
- 中文时应包含 `Accept-Language: zh-CN`
- 英文时应包含 `Accept-Language: en-US`

---

## 任务 13：运行所有测试

- [ ] **步骤 1：运行后端单元测试**

运行：
```bash
cd harnax-admin && mvn test
```

预期：所有测试 PASS，包括 I18nConfigTest 和 MessageUtilTest

- [ ] **步骤 2：运行前端测试（如果有）**

运行：
```bash
cd harnax-webui && npm test
```

- [ ] **步骤 3：验证应用启动**

运行：
```bash
cd harnax-admin && mvn spring-boot:run
```

预期：应用正常启动，无报错

---

## 任务 14：最终 Commit

- [ ] **步骤 1：创建最终 commit**

```bash
git add -A
git commit -m "feat(i18n): 完整实现前后端国际化支持

- 后端：I18nConfig、MessageUtil、消息资源文件
- 后端：迁移所有核心 Service 的错误消息
- 前端：Axios 拦截器添加 Accept-Language 请求头
- 前端：补充完整中英文国际化文案
- 测试：添加 I18nConfig 和 MessageUtil 单元测试
- 文档：添加国际化使用方式说明

成功标准：
✅ 后端正确解析 Accept-Language 请求头
✅ 错误消息根据语言动态切换（中英文）
✅ 前端语言切换后，所有 API 错误消息正确翻译
✅ 所有单元测试和集成测试通过
✅ 国际化文案检查清单完成"
```

---

## 成功标准检查清单

✅ 后端配置
- [ ] I18nConfig 正确配置 MessageSource 和 LocaleResolver
- [ ] MessageUtil 工具类可用
- [ ] 消息资源文件包含所有错误消息

✅ 后端迁移
- [ ] AuthServiceImpl 错误消息已迁移
- [ ] TenantInterceptor 错误消息已迁移
- [ ] SysUserServiceImpl 错误消息已迁移
- [ ] UserTenantServiceImpl 错误消息已迁移
- [ ] TenantServiceImpl 错误消息已迁移

✅ 前端同步
- [ ] Axios 拦截器添加 Accept-Language 请求头
- [ ] 前端国际化文案完整

✅ 测试
- [ ] I18nConfigTest 通过
- [ ] MessageUtilTest 通过
- [ ] 所有单元测试通过
- [ ] 端到端测试通过（中英文）

✅ 代码质量
- [ ] 遵循命名规范（error.{模块}.{实体}.{操作}）
- [ ] 无硬编码错误消息残留
- [ ] 代码审查通过
